[build]
builder = "DOCKERFILE"
# Railway: leave the service Root Directory as "/" (repo root) in the dashboard
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
